state/
data/
.env
*.log
*.png
*.jpg
*.jpeg
*.webp
config/openclaw.json.bak*

# workspace/reports is versioned (briefings are intentional output we want to ship).
# workspace/data, workspace/state etc. stay out via the root .gitignore.
